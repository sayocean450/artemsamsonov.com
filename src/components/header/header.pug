mixin header(theme='light', modeValue='inactive')
    include ../icon/icon
    div.header(class='header_' + theme)
        div.header__logo
            a.header__logo-link(href="index.html") артём самсонов
        div.header__menu
            a.header__menu-elem(href="#writing") портфолио
            a.header__menu-elem(href="#contacts") контакты
