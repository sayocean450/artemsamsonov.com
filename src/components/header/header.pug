mixin header(theme='light', modeValue='inactive')
    include ../icon/icon
    div.header(class='header_' + theme)
        div.header__logo
            a.header__logo-link(href="index.html") артём сошников
        div.header__menu
            a.header__menu-elem(href="index.html#writing") проза
            a.header__menu-elem(href="index.html#contacts") контакты
